# Optional waveform plotting dependency; C++ simulator and Python runner need only
# a C++17 compiler and the Python standard library.
matplotlib>=3.5

# Tkinter is provided by your Python installation or OS (e.g. python3-tk on Debian),
# not by the tkinter name on PyPI.
